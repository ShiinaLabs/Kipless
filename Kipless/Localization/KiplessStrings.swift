import Foundation

/// Kept as a source-file placeholder for the localization namespace used by
/// older drafts of the app.
///
/// App code now references the generated `LocalizedStringResource` symbols
/// directly. This lets Xcode associate each catalog entry with its source
/// reference while keeping the semantic key and its English fallback in
/// `Localizable.xcstrings`.
enum KiplessStrings {}
